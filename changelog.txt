= 1.5.8 =
* Fixed Plugin Check findings for discouraged manual text-domain loading, POST helper nonce/input analysis, and .htaccess writable diagnostics.

= 1.5.7 =
* Removed the three-card settings guide from the admin settings form.
* Moved the Save Settings & Regenerate Rules button out of the collapsed Health Check section by adding a standalone settings action bar.
* Removed the selected Cookie-mode / pre-PHP explanatory notices from the admin interface.
* Updated admin accordion JavaScript so the standalone action bar is not captured inside the last settings accordion.

= 1.5.6 =
* Fixed Persian translation strings that displayed literal Unicode escape sequences instead of real zero-width non-joiner characters.
* Recompiled the bundled fa_IR MO file.

= 1.5.5 =
* Fixed real cookie Health Check tests so they no longer use the bypass token. Added a separate bypass-token test.
* Fixed regex POST handling to preserve backslashes.
* Added .htaccess diagnostics with path, writable/readable status, managed block detection, hashes, filesystem method, and last operation result.
* Split .htaccess rules: filtered URL pre-PHP blocking is Strict/Emergency only; XML-RPC blocking is independent.
* Implemented compatible cookie-name regex in PHP while keeping HMAC validation authoritative.
* Added trusted proxy / real IP handling for Cloudflare and custom CIDRs.
* Localized admin JS labels and moved older changelog details to changelog.txt.

= 1.5.4 =
* Hardened .htaccess write/remove handling with a local filesystem fallback for hosts where WP_Filesystem cannot update the root .htaccess file.

= 1.5.3 =
* Fixed stale .htaccess cleanup so Cookie modes remove managed pre-PHP filter blocks during save and manual rewrite actions.

= 1.5.2 =
* Limit filtered URL pre-PHP server blocking to Strict and Emergency modes to avoid false blocking real shoppers in Cookie modes.

= 1.5.1 =
* Avoided Apache pre-PHP cookie-mode false blocks by handling Cookie and Cookie + Referer filtered URL decisions in PHP; Strict and Emergency still use pre-PHP blocking.

= 1.5.0 =
* Added bundled Persian (fa_IR) PO/MO translation files and registered the plugin text domain path for reliable local translation loading.

= 1.4.9 =
* Hardened Apache/LiteSpeed cookie pre-check rules by reading the raw Cookie header and using a broader plugin-owned cookie-name match while PHP still validates HMAC signatures.

= 1.4.8 =
* Fixed Plugin Check escaping warnings in admin tooltip/section heading output.
* Kept the collapsible settings UI and runtime protection behavior unchanged.

= 1.4.7 =
* Reduced settings-page visual clutter with collapsible settings sections.
* Added a compact settings guide and expand/collapse controls for advanced options.
* Made per-setting help icons quieter by default while preserving hover/focus tooltips.

= 1.4.6 =
* Clarified Live Dashboard scope: counters now explicitly show PHP-level events only.
* Added an admin notice explaining that .htaccess pre-PHP blocks are visible in server logs, not WordPress event logs.
* Added generated .htaccess comments documenting pre-PHP logging limits.

= 1.4.5 =
* Added accessible, keyboard-focusable admin tooltips for every settings section and every configurable option.
* Improved admin help icon styling with WordPress Dashicons and responsive tooltip positioning.

= 1.4.4 =
* Shortened Upgrade Notice entries so every notice stays below the WordPress.org 300-character limit.
* No runtime protection behavior changed from 1.4.3.

= 1.4.3 =
* Improved RTL admin layout, table alignment, and code block readability.
* Auto-fills and saves the Health Check test path from the WooCommerce shop/category path when possible.
* Added stricter Cookie + Referer mode guidance and clearer separate Cloudflare regex/fallback copy instructions.

= 1.4.2 =
* Declared WooCommerce HPOS/custom order tables compatibility.
* Improved Health Check with an internal temporary token so signed cookies bound to User-Agent/IP can be tested reliably.
* Auto-detects a WooCommerce shop/category test path when the health-check path is empty.
* Fixed Cloudflare args.names syntax and added a no-regex Cloudflare fallback expression.

= 1.4.1 =
* Renamed the main plugin class from lowercase prefix style to PascalCase prefix style: Filter_Guard_For_WooCommerce_Plugin.
* Kept all 1.4.0 hardening changes intact.

= 1.4.0 =
* Count all expensive filtered requests, not only blocked requests, for Auto Emergency pressure detection.
* Align Emergency `.htaccess` query matching with the configured query key regex.
* Generate Cloudflare query rules from `http.request.uri.args.names` instead of broad query-string contains checks.
* Tighten Cloudflare cookie pre-check with a stable signed-cookie regex pattern for rotated cookie names.

= 1.3.9 =
* Made generated Apache/LiteSpeed, Nginx, and Cloudflare cookie checks stable when daily signed-cookie name rotation is enabled.
* Expanded generated Cloudflare query matching to follow configured expensive query keys beyond filter_ and query_type_.
* Clarified that server/CDN rules only pre-check plugin-owned cookie name patterns; PHP validates HMAC signatures.

= 1.3.8 =
* Renamed the main plugin class to start with the exact plugin prefix expected by WordPress Coding Standards / Plugin Check.
* Kept all 1.3.7 hardening changes intact.

= 1.3.7 =
* Added update-time database schema migration, multisite-aware lifecycle cleanup, signed-cookie-only server rule generation, and Nginx internal-data deny snippets.

= 1.3.6 =
* Added public-root compatibility using get_home_path(), hardened Cloudflare expressions, clarified best-effort rate limits, removed regex suppression, and improved filesystem helper usage.

= 1.3.5 =
* Made Monitor mode neutral, improved server rule generation, added GET-based health checks, decoupled Auto Emergency from event logging, and hardened NDJSON logging.

= 1.3.4 =
* Guarded manual .htaccess rewrites in safe modes, added health-check test paths, hardened rule generator safe-mode output, and improved uninstall warnings.

= 1.3.3 =
* Hardened Monitor mode so it does not block filtered traffic, XML-RPC, or rate-limit by default.
* Reworked rate limiting to use short-lived WordPress transients/object cache counters instead of JSON file counters.
* Added event-log sampling and emergency-mode per-request log suppression to reduce database pressure during attacks.
* Made Health Check mode-aware and stricter.
* Moved backups and NDJSON events outside public uploads.
* Added optional direct canonical tag output toggle.
* Updated managed .htaccess/robots.txt markers while preserving legacy marker cleanup.

= 1.3.2 =
* Fixed Plugin Check errors around translator comments and CSV output context.

= 1.3.1 =
* Renamed plugin and slug to avoid restricted/trademarked terms at the beginning of the name.
* Hardened SQL preparation, filesystem operations, nonce/sanitize handling, and readme metadata.

= 1.3.0 =
* Added event logging, dashboard stats, signed cookies, bot verification, complexity scoring, rate limiting, emergency mode, rule generators, health checks, and rollback.
